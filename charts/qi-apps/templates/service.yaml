apiVersion: v1
kind: Service
metadata:
  name: {{ include "qi-apps.fullname" . }}
  labels:
    {{- include "qi-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qi-apps.selectorLabels" . | nindent 4 }}
