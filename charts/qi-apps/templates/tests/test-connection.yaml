apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qi-apps.fullname" . }}-test-connection"
  labels:
    {{- include "qi-apps.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qi-apps.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
